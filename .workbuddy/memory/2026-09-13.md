# 2026-09-13

## ERC-8004 学习与课程展示准备
- 用户为课程展示研究 ERC-8004（Trustless Agents）标准，先后阅读两篇文章（datawallet.com 解析 + 知乎 Stephen 技术解析）。
- 用 Visualizer 输出过：三大注册表结构图、五大担忧→扩展方向对照图、实操生命周期总览、8 步交互演示。
- 新建 `erc8004-demo/index.html`：单文件离线演示应用，模拟三个注册表（Identity/Reputation/Validation），含模拟链事件日志（Explorer）、注册/发现/委托/反馈/验证全流程、双评分模型（市场评分 vs 保险信任分）、演讲脚本页。数据存 localStorage，可重置。预置 2 个种子 Agent（PriceOracle Bot、TradeExecutor）。
- 项目方向：TrustLessAgent 与 ERC-8004 高度相关，后续可能深入对接。
- 演示应用升级为中英双语（zh/en 切换按钮 + data-i18n 静态层 + t() 动态层 + localStorage 记忆语言；种子数据用 descKey/noteKey 本地化）。
- 用户疑问：任务执行无交易因 A2A/MCP 本是链下通信；任务类型仅演示预填，已加自定义输入框。
- 新建 `agent-platform/`（本地注册平台+交易市场，零依赖 Node）：server.js 端口 8800（注册/技能上架/订单托管结算/反馈/账本/档案页），agents/demo-agent.js 自托管主页+agent-card.json+自动注册心跳+真实执行接口，支持多实例（NovaBot 8801 / LyraBot 8802）。本地积分 LGC 模拟 x402，新 Agent 空投 200。数据存 data/state.json，重置后 Agent 心跳自动重注册（15s，幂等）。
- `sepolia/`：create-wallet.cjs + register.cjs（真实调用 Sepolia IdentityRegistry 0x8004A818BFB912233c491871b3d84c89A494BD9e，注册 JSON 用 data URI 免 IPFS），演示钱包 0xd504Ab47FBc7CD9eD103039CEdE86Afdb118E61F 已生成待领水（用户需自领 Sepolia ETH）。ethers 装在 managed node workspace。一键启动：agent-platform/启动AgentHub.bat。
- 主网规范地址 IdentityRegistry 0x8004A169FB4a3325136EB29fA0ceB6D2e539a432，Sepolia 0x8004A818BFB912233c491871b3d84c89A494BD9e，跨链同址确定性部署。
